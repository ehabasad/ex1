package pac;

/**
 * An interface representing an object that can be printed to standard output.
 */
public interface Printable {
    /**
     * Prints a complete message about the object.
     */
    void print();
}
